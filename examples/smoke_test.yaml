name: "ACP Smoke Test"
version: "0.1.0"

nodes:
  - id: hello
    name: "Hello World"
    execute:
      url: "examples/prompts/hello.md"
settings:
  presets:
    default:
      transport: acp
      provider: anthropic
      model: "sonnet"
      default: true
