Generate exactly 5 short, funny jokes. Respond with ONLY valid JSON matching this exact schema — no markdown, no code fences, no extra text:

{"jokes": ["joke 1", "joke 2", "joke 3", "joke 4", "joke 5"]}

Each joke should be a single string. Do not include numbering or prefixes.