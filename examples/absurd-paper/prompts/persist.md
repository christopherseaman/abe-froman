## Task

Persist the assembled paper to disk. **Plumbing only** — do not modify, summarize, or comment on the content.

Use the **Write** tool with path `../../paper/paper.md` (relative to your git worktree root — the `../../` reaches the workflow's shared staging directory).

The content to write is the paper markdown below. Copy it verbatim as the `content` parameter to Write. No edits.

### Content

```
{{reconcile}}
```

After the Write succeeds, respond with a single word: `persisted`. Do not echo or summarize the content.
